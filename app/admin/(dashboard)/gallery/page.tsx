export default function AdminGalleryPage() {
  return (
    <div>
      <h1 className="font-serif text-2xl">Gallery</h1>
      <p className="text-sm text-zinc-600">Wire `POST /api/upload` to Cloudinary (see env variables). Upload UI can drop here next.</p>
    </div>
  );
}
